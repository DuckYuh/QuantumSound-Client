import { Loading } from "@/components/ui";

export default function LoadingProtected() {
    return (
        <Loading
            title="Loading your library..."
            description="Preparing your music."
        />
    );
}