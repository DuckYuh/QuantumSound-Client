import { Loading } from "@/components/ui";

export default function AppLoading() {
    return (
        <Loading
            title="Loading..."
            description="Please wait a moment."
        />
    );
}